#ifndef SERVER
#define SERVER

#include <sys/socket.h>
#include <sys/types.h>
#include <netdb.h>

class Server {
    private:
    public:
        Server();  // Constructor
        ~Server(); // Destructor
};

#endif
